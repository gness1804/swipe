import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  ball: {
    height: 60,
    width: 60,
    borderRadius: 30,
    borderWidth: 30,
    borderColor: '#000',
  },
});

export default styles;
